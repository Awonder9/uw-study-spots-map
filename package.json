{
  "name": "uw-study-spots-map",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dev:full": "npm run build && wrangler pages dev dist --kv STUDY_SPOTS_KV --compatibility-date=2026-09-07"
  },
  "devDependencies": {
    "vite": "^8.0.1",
    "wrangler": "^4.129.1"
  }
}
